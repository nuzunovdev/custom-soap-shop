services:
  postgres:
    image: postgres:16
    container_name: custom-soap-shop-db
    environment:
      POSTGRES_DB: custom_soap_shop
      POSTGRES_USER: "master_user"
      POSTGRES_PASSWORD: "FuckingEnergy23@#"
    ports:
      - "5433:5432"
    volumes:
      - custom_soap_shop_data:/var/lib/postgresql/data

volumes:
  custom_soap_shop_data: